diff --git a/node_modules/react-native-manage-wallpaper/android/build.gradle b/node_modules/react-native-manage-wallpaper/android/build.gradle
index fe473d8..bf6fe19 100644
--- a/node_modules/react-native-manage-wallpaper/android/build.gradle
+++ b/node_modules/react-native-manage-wallpaper/android/build.gradle
@@ -33,7 +33,7 @@ buildscript {
     if (project == rootProject) {
         repositories {
             google()
-            jcenter()
+            mavenCentral()
         }
         dependencies {
             classpath 'com.android.tools.build:gradle:3.4.1'
@@ -80,7 +80,7 @@ repositories {
         url "$rootDir/../node_modules/jsc-android/dist"
     }
     google()
-    jcenter()
+    mavenCentral()
 }
 
 dependencies {
@@ -120,39 +120,39 @@ def configureReactNativePom(def pom) {
     }
 }
 
-afterEvaluate { project ->
-    // some Gradle build hooks ref:
-    // https://www.oreilly.com/library/view/gradle-beyond-the/9781449373801/ch03.html
-    task androidJavadoc(type: Javadoc) {
-        source = android.sourceSets.main.java.srcDirs
-        classpath += files(android.bootClasspath)
-        include '**/*.java'
-    }
-
-    task androidJavadocJar(type: Jar, dependsOn: androidJavadoc) {
-        archiveClassifier = 'javadoc'
-        from androidJavadoc.destinationDir
-    }
-
-    task androidSourcesJar(type: Jar) {
-        archiveClassifier = 'sources'
-        from android.sourceSets.main.java.srcDirs
-        include '**/*.java'
-    }
-
-    android.libraryVariants.all { variant ->
-        def name = variant.name.capitalize()
-        task "jar${name}"(type: Jar, dependsOn: variant.javaCompile) {
-            from variant.javaCompile.destinationDir
-        }
-    }
-
-    artifacts {
-        archives androidSourcesJar
-        archives androidJavadocJar
-    }
-
-    task installArchives(type: Upload) {
-        configuration = configurations.archives
-    }
-}
+// afterEvaluate { project ->
+//     // some Gradle build hooks ref:
+//     // https://www.oreilly.com/library/view/gradle-beyond-the/9781449373801/ch03.html
+//     task androidJavadoc(type: Javadoc) {
+//         source = android.sourceSets.main.java.srcDirs
+//         classpath += files(android.bootClasspath)
+//         include '**/*.java'
+//     }
+// 
+//     task androidJavadocJar(type: Jar, dependsOn: androidJavadoc) {
+//         archiveClassifier = 'javadoc'
+//         from androidJavadoc.destinationDir
+//     }
+// 
+//     task androidSourcesJar(type: Jar) {
+//         archiveClassifier = 'sources'
+//         from android.sourceSets.main.java.srcDirs
+//         include '**/*.java'
+//     }
+// 
+//     android.libraryVariants.all { variant ->
+//         def name = variant.name.capitalize()
+//         task "jar${name}"(type: Jar, dependsOn: variant.javaCompile) {
+//             from variant.javaCompile.destinationDir
+//         }
+//     }
+// 
+//     artifacts {
+//         archives androidSourcesJar
+//         archives androidJavadocJar
+//     }
+// 
+//     task installArchives(type: Upload) {
+//         configuration = configurations.archives
+//     }
+// }
